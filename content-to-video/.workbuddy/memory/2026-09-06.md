# 2026-09-06

## content-to-video 实例制作：MCP 入门讲解
- 项目目录：`C:\Users\Hopper\Documents\CODE\videos\mcp-intro\`（产物不入技能目录）
- 选题：MCP 是什么（USB-C 比喻），信源 modelcontextprotocol.io（introduction + architecture），完整讲解模式
- 34 句 TTS（冰糖主音色，seg3 双人对话 茉莉/苏打），121.5s；配图 6 段全 ImageGen（landscape_4_3，dark navy + flat vector style，30-60 credits）
- 渲染：横屏 1920×1080 bar 模式；check 0 error；verify 通过（H.264+AAC，时长偏差 0.03s）
- 踩坑：① hyperframes render 必须在 composition 目录（hf-project）下执行，从项目根跑会报 "No composition found"（render_watch 的 -o 相对路径跟着 cwd 走）② 技能文件只读，改前 chmod u+w；preview.js 只读拷贝是复发性坑（copy2 保留源权限，二次生成 HTML 必 PermissionError）——已在 gen_hyperframes.py 修复（覆盖前 chmod 0o644）

## 第二轮布局优化（用户 4 点，全部改入技能源码）
1. **画幅收敛**：只留 landscape(1920×1080) + portrait(1080×1440, 3:4)；`--aspect vertical`(9:16) 从 CLI 删除，非紧凑竖屏死分支一并移除（generate_html 签名去掉 v_compact）；both 的竖屏版仍叫 out.vertical.mp4 但内容为 3:4；`subtitle_params_for` 补了 portrait→vertical 归一化（否则 export_extras 传 portrait 会错拿横屏切行参数）
2. **开场标题上移**：template.json landscape.title.topOther 16%→10%（容纳更多目录）
3. **目录软上限 7 条**：模板 agenda.maxItems=7，超出截前 7 条 + stderr warn（突破=调大模板值）；不加"等共 N 条"提示行（历史反模式）
4. **图框 √2:1**：860×700(1.229)→860×608(√2)，top 550→596（下缘贴字幕条 900、上缘 292 避两行标题）；4:3 生图填充 94%、16:9 素材 80%，比 4:3 槽/旧槽都更均衡——这是采纳用户提议的依据；"两行标题伸入图片区"告警随前提失效一并删除，标题行数估算保留（正文卡动态 top 用）
- 验证：selftest 531/533（2 个 run_eval h264 失败为环境性、改动前即红）、dev/check.py 17/19（同因）、MCP 项目实测抓帧 0 error、成片重渲染 verify 通过
- 文档同步：SKILL.md（画幅节/图框/maxItems/sub-mode 节/export --aspect）、rendering.md、image_options.md、dev/evals.json

## 布局改动（用户要求的 3 点，已改技能源码）
1. **默认主题 cream**：`gen_hyperframes.py --theme` 默认本就是 cream（dark 是当时手动传的）；用户明确"以后默认 cream"，不要擅自换主题
2. **内容框左缘 50px**：`config/template.json` landscape.body 新增 `leftMargin: 50`（此前对齐标题文字 205）
3. **内容框与标题间距 50px**：新增 `titleGap: 50`——top 从固定锚点 368（两行标题最坏情况+30px）改为"标题组底部（按标题估算行数动态推导）+ titleGap"，单行标题时 top 368→297；`sideGap: 50` 参数化卡片右缘与图片左缘间距；maxWidth 870→910
- bar 正文卡与 verse 句子流窗口共用同一套 `_box_*` 几何推导；`selftest.py` 4 处旧几何断言已同步（531/533，剩 2 个 run_eval h264 枚举失败为环境性、改动前即红）

## 砍掉横屏 verse 模式（1.5.48）
- 用户决策：横屏 verse 实际出片不好看，直接砍掉；以后无模式区分——横屏固定 bar、竖屏固定 verse。
- 改动：gen_hyperframes.py 删 --sub-mode 与横屏 verse 全部死分支（标题居中、#opening/.verse CSS、独立盒/流内盒两套定位），generate_html 内部按 aspect 派生 sub_mode；run.py/export_extras.py 删参数透传；subtitle_params_for 收窄为只按 aspect；check_series.py 不再对比 params.sub_mode。
- selftest 523/525（2 个既有 h264 环境项）、dev/check 17/19；用 mcp-intro 实测横屏 HTML 生成 + export_extras 正常。
- 教训：同文件多个 Edit 并行调用会互相覆盖（读-改-写竞态），必须串行编辑——本轮因此返工 5 处。

## 去 PPT 感 + 排版参数收编模板（1.5.49）
- 用户反馈：①改排版要同时动 template 和多个 py(耦合高)；②内容框文字与字幕字号几乎一致+底色卡片盒太像 PPT。
- 视觉：body.card 开关默认 false(裸文字无卡片盒)；横屏正文字号 44→38,与字幕 46 拉开层级;num-accent 浅底 darken/深底提亮(修 #ef5350 在 cream 上 2.79:1 对比度违规)。
- 去耦合：竖屏 segCard.padding/image.aspect/image.marginTop/verse.windowHeight/verse.clipPad 全部收进 template.json(原先硬编码在 gen_hyperframes)。
- 坑：selftest 模板键消费检查要求 layout 值为 dict,字符串键(compactPadding)会崩——按 segCard/verse 子块组织。SKILL.md frontmatter version 与 changelog current_version 必须同步 bump,漏了会挂 packaging:version_consistency。
- 验证：selftest 523/525(既有环境项)、check 0 error、对比度 51/51、mcp-intro 快照逐帧目检。

## 卡片回归（1.5.49 修正）
- 用户目检否决裸文字方案："没卡片直接放在背景上太丑"。body.card 默认改回 true,卡片保留为默认外观。
- 保留项：字号层级(正文 38 < 字幕 46)、模板参数化(segCard/verse/aspect 等收编 template.json)、num-accent 对比度加固。
- 教训：视觉改版先出一版快照给用户看再落默认值;去盒子这类"减法"改动用户不一定买账,卡片底在这些主题里承担可读性功能。

## 排版与代码彻底解耦（1.5.50）
- selftest 排版断言全部改为模板推导(竖屏 padding/aspect/margin/clipPad、横屏图片槽 top/width/height、标题 left/width/top),不再写死数值;“整屏居中”判据改 ==1080/2 表达语义。
- gen_hyperframes 最后一批硬编码收编:title.rightInset(110/60)、agenda.itemGap(14)、chip.gap(16)、verse.linePad(5)、vertical.image.borderRadius(16);非 badge 配图段标题左缘对齐 body.leftMargin。
- 验收实验:脚本改模板 10 处 → selftest 零改动全绿;唯一拦截是 √2:1/sideGap 关系不变式(设计守护,非数值耦合)。
- 坑:模板键消费检查要求 gen 源码以双引号引用键名,f-string 内层单引号会漏检(报 dead keys)——用先行变量接模板值。

## 奥卡姆剃刀瘦身落地（1.5.51）
- 全砍（用户确认）：wiki 自进化全家桶 5 脚本 + _trace.py + dev/wiki/（~2600 行）、visual_regression.py；check.py 3 道 wiki 门禁与 selftest ~110 条断言同步拆除；run.py 轨迹旁路（--no-trace/atexit/_note_outcome×12）全拆。
- run_eval 三项加固：假 ffmpeg 替身 Windows 下出 .bat（h264 枚举永久红转真绿）；损坏图检查加 Pillow 条件守卫（venv 有 Pillow 走真校验、裸运行时 SKIP）；并行计时用例 sleep 1.8/1.0 + 重试 3 次（Defender 扫描临时文件致 spawn 变慢在 check.py 上下文稳定假红）。
- **重大事故**：SKILL.md 被我编辑脚本里的贪婪正则（re.S 下 `.*└── proposals/.*\n` 吞到文件尾）截断至 71 行。从 git HEAD（今日 11:24 提交的 1.5.47）恢复后重放当日增量（verse 砍除文案、画幅绑定模式、Occam 删节、版本 1.5.51），门禁（toc_anchors/scope_declaration/tagline_fallback）全数恢复。
- 教训：①批量文本手术禁用贪婪正则跨行匹配，用行号定位 + 首尾断言；②git 提交是救命稻草——本轮若 HEAD 里没有 11:24 的提交，今天 1.5.48-1.5.50 的文档工作全部丢失；③建议每次大改后立即 commit。
- 最终验证：dev/check 16/16、selftest 419/419、run_eval 28/28，venv（有 Pillow）与裸运行时双口径全绿。

## 再砍两个可选模块（1.5.52）
- 用户点名：export_extras.py（chapters.txt + SRT 导出）、gen_cover.py（竖版封面+标题草稿）删除——平时生成从不用。
- 清理面：SKILL.md（树/工作流命令/封面段）、internals/rendering 两处、run_eval [4] 子进程测试、selftest [13] 整节+16c+⑨+两消费者共用参数断言（收窄为 gen_hyperframes 单消费者）、4 个核心脚本注释口径。subtitle_params_for 保留（gen_hyperframes 仍消费）。
- 并行计时用例再加固：venv 下 spawn 开销稳定 +0.6s（1.8 基准+2.3 窗口重试也救不了——开销是稳态不是抖动），基准拉到 2.5s/窗口 3.2s 后连续全绿。教训：**计时类断言的窗口必须 ≥ 实测稳态开销×2**，先量开销再定窗口，别靠重试。
- 坑：bump 版本时只改了 frontmatter 忘了 changelog current_version，version_consistency 门禁正确拦截——两处必须原子改。
- 验证：dev/check 双运行时 16/16（bare 398/398+22/22、venv 398/398+25/25）。
