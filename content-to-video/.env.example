# API 密钥占位模板（本文件无真实密钥，可直接提交/分发）
#
# 用法：把本文件内容抄到 ~/.config/ai-video/.env（推荐，用户级、分享技能不泄露），
# 或导出为系统环境变量。脚本不会读取技能目录里的 .env——
# 加载顺序见 scripts/_env.py：环境变量 > ~/.config/ai-video/.env。
#
# 变量清单（也是 _env.py 实际支持的全部变量，勿凭空添加）：
#
# MiMo TTS 配音（第 3 步，免费模型；缺 Key 时 pipeline.py 直接报错退出）
MIMO_API_KEY=sk-xxxxx
# 可选：TTS 模型名（默认 mimo-v2.5-tts）
#MIMO_TTS_MODEL=mimo-v2.5-tts
# 可选：MiMo API 地址（默认 https://api.xiaomimimo.com/v1）
#MIMO_BASE_URL=https://api.xiaomimimo.com/v1
#
# StepFun 文搜图（第 4 步方式 A；缺 Key 时 run.py 跳过搜图、无已定稿配图则退回纯文字版，方式 B/C/D 不依赖此 Key）
STEPFUN_API_KEY=xxxxx
